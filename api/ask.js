# in a terminal
mkdir dasara-voice-kiosk && cd dasara-voice-kiosk
mkdir api
# create files with your editor, paste the contents below
git init
git add .
git commit -m "init"
# push to GitHub (replace USERNAME and created repo URL)
git branch -M main
git remote add origin https://github.com/USERNAME/dasara-voice-kiosk.git
git push -u origin main
